.terraform/
.terraform/*
**/.terraform/*
*.tfstate
*.tfstate.*.backup
*.tfvars
*.tfvars.json
crash.log
*.md

.terraform/
.terraform/*
**/.terraform/*
*.tfstate
*.tfstate.*.backup
*.tfvars
*.tfvars.json
crash.log
*.md

# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*.backup

# Crash log files
crash.log

# Exclude all .tfvars files, which might contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version control
# as they are data-specific to the user.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# should not be checked into source control
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore any .env or .dockerenv files that may contain environment variables
.env
.dockerenv
